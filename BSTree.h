#ifndef BSTREE_H
#define BSTREE_H
#include <iostream>
/*
template <class Key>
class BSTree
{
    Key value;
    BSTree *parent;
    BSTree *l_son;
    BSTree *r_son;
    BSTree *root;
    public:
    BSTree* most_left(BSTree* start);
    BSTree* lookForVal(BSTree* start, Key value);
    int addNode(Key n, BSTree* start);
    void delNode(BSTree *start);
    void in_order_tree_walk(BSTree* start);
};
*/
#endif // BSTREE_H
